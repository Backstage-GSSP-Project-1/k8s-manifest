apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
spec:
  type: NodePort
  selector:
    app: ${APP_NAME}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30017   # You can change this (range: 30000–32767)
      
# apiVersion: v1
# kind: Service
# metadata:
#   name: ${APP_NAME}
# spec:
#   selector:
#     app: ${APP_NAME}
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 3000

#   type: LoadBalancer

